import SwiftUI

// MARK: Row
enum RowToken {
    static let minHeight: CGFloat = 48
    static let radius = Radius.xs
    static let spacing = Spacing.reg
    static let padding = Spacing.reg
    static let dividerStandardPadding: CGFloat = Spacing.reg
    static let dividerIconPadding: CGFloat = 61
}
